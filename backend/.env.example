# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=tlgdb
DB_USER=root
DB_PASSWORD=
DB_DIALECT=mysql

# API
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Auth (JWT en segundos; por defecto 900 = 15 min)
JWT_EXPIRES_SEC=900

# Initial admin (seed only)
SEED_ADMIN_EMAIL=admin@tlo.mx
SEED_ADMIN_PASSWORD=ChangeMe123!
